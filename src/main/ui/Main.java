package ui;

// Main class which includes the main method that
// inits the CLI
public class Main {
    public static void main(String[] args) throws Exception {
        TerminalGame gameHandler = new TerminalGame();
        gameHandler.start();
    }
}
